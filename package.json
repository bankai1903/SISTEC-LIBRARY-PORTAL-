{
  "name": "library-management-system",
  "version": "1.0.0",
  "description": "Library Management System with User and Admin Panels",
  "scripts": {
    "client": "npm run dev --prefix client",
    "dev": "npm run client",
    "install-all": "npm install --prefix client --include=dev --legacy-peer-deps",
    "build": "npm run install-all && npm run build --prefix client"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
